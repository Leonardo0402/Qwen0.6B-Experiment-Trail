bf16: true
dataloader_num_workers: 0
eval_file: data/splits/validation.jsonl
eval_steps: 10
fp16: false
gradient_accumulation_steps: 8
gradient_checkpointing: true
learning_rate: 0.0001
logging_steps: 5
lora:
  alpha: 64
  dropout: 0.05
  rank: 32
  target_modules:
  - q_proj
  - k_proj
  - v_proj
  - o_proj
  - gate_proj
  - up_proj
  - down_proj
lr_scheduler_type: cosine
max_grad_norm: 0.3
max_seq_length: 512
model_name_or_path: models/Qwen3-0.6B
num_train_epochs: 3
output_dir: adapters/code-lora-v3-boundary
per_device_eval_batch_size: 1
per_device_train_batch_size: 1
report_to: none
save_steps: 50
save_total_limit: null
seed: 42
train_file: data/splits/train.jsonl
use_cache: false
warmup_ratio: 0.03
weight_decay: 0.0
