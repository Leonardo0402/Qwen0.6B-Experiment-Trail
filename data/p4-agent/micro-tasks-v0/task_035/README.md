# Task: task_035

## Goal
Fix the bug in clip_to_range(). The obvious fix may not work — inspect errors and retry.

## Constraints
- Do not modify test_*.py files

## Hints
- The function has the comparison directions wrong AND returns the wrong bound.
