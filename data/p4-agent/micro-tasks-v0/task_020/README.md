# Task: task_020

## Goal
Update the format_phone() helper to match the spec: return '(XXX) XXX-XXXX' format.

## Constraints
- Do not modify test_*.py files

## Hints
- The function currently returns the raw digit string; it should format with parens and dash
