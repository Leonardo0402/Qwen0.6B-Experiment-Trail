# Task: task_033

## Goal
Fix the bug in limit_value(). The obvious fix may not work — inspect errors and retry.

## Constraints
- Do not modify test_*.py files

## Hints
- The function has the return values swapped AND an off-by-one in the in-range case.
