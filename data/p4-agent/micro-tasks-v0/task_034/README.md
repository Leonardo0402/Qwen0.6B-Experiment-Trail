# Task: task_034

## Goal
Fix the bug in bound_value(). The obvious fix may not work — inspect errors and retry.

## Constraints
- Do not modify test_*.py files

## Hints
- The function has the comparison directions wrong for both bounds.
