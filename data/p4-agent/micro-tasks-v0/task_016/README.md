# Task: task_016

## Goal
Update the format_price() helper to match the spec: return '$X.XX' format with 2 decimal places.

## Constraints
- Do not modify test_*.py files

## Hints
- The function currently returns an integer; it should return a formatted string
