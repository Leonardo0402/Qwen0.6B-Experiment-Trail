# Task: task_017

## Goal
Update the format_percent() helper to match the spec: return 'XX.X%' format with 1 decimal place.

## Constraints
- Do not modify test_*.py files

## Hints
- The function currently returns a float; it should return a formatted string
