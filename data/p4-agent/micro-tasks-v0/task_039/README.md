# Task: task_039

## Goal
Fix both bugs in fractionalize() so all tests pass. A single fix may not be enough.

## Constraints
- Do not modify test_*.py files
- Run tests after each patch to verify progress

## Hints
- There are two bugs: one in the zero-length branch and one in the divisor
