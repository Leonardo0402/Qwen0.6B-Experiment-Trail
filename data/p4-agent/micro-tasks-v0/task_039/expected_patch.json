{
  "file_path": "solution.py",
  "old_text": "    if n == 0:\n        return values\n    return [v / (n - 1) for v in values]",
  "new_text": "    if n == 0:\n        return []\n    return [v / n for v in values]"
}
