# Task: task_038

## Goal
Fix both bugs in rescale() so all tests pass. A single fix may not be enough.

## Constraints
- Do not modify test_*.py files
- Run tests after each patch to verify progress

## Hints
- There are two bugs: one in the condition and one in the return value
