# Task: task_031

## Goal
Fix the bug in clamp(). The obvious fix may not work — inspect errors and retry.

## Constraints
- Do not modify test_*.py files

## Hints
- The function should return lo when x < lo, hi when x > hi, else x. But the current code has the comparison directions wrong AND a logic error.
